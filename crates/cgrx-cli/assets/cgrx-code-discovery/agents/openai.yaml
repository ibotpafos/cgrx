interface:
  display_name: "CGRX Code Discovery"
  short_description: "Verify code changes with the CGRX graph"
  default_prompt: "Use $cgrx-code-discovery to investigate this code task with bounded graph evidence."

dependencies:
  tools:
    - type: "mcp"
      value: "cgrx"
      description: "Local multi-repository CGRX MCP server"

policy:
  allow_implicit_invocation: true
