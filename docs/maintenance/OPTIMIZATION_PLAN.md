# Optimization plan

Tracked optimization areas:

- retrieval allocations and cache behavior;
- index load and refresh latency;
- snapshot memory footprint;
- deterministic concurrent access;
- release artifact reproducibility.

Every optimization must keep evidence correctness tests passing.
